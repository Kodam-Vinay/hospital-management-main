export type navigationLinkTypes = {
  path: string;
  name?: string;
  element?: React.ReactNode;
  color?: string;
};

export type ContactFormValues = {
  name: string;
  email: string;
  phone: string;
  message: string;
};
